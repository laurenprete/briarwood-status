{
  "name": "briarwood-status-infra",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "tsc",
    "synth": "cdk synth",
    "deploy": "cdk deploy --profile cdk-admin --require-approval never"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "aws-cdk": "^2.150.0"
  },
  "dependencies": {
    "aws-cdk-lib": "^2.150.0",
    "constructs": "^10.0.0"
  }
}
